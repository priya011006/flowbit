{
  "name": "flowbit",
  "version": "1.0.0",
  "private": true,
  "description": "Flowbit Analytics Dashboard with Chat with Data",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "ingest": "ts-node -r dotenv/config scripts/ingest.ts",
    "db:push": "prisma db push",
    "db:generate": "prisma generate",
    "db:studio": "prisma studio"
  },
  "workspaces": [
    "apps/*",
    "services/*"
  ],
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "@types/node": "^24.10.0",
    "prisma": "^6.19.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3",
    "turbo": "^2.3.3"
  }
}
